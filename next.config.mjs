/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["raw.githubusercontent.com", "picsum.photos", "rickandmortyapi.com"]
    }
};

export default nextConfig;
